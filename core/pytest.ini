[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
testpaths = tests
markers =
    integration: requires running ZizkaDB stack (docker compose up on :8000)
    requires_openai: needs OPENAI_API_KEY for embedding tests
    security: auth and tenant isolation tests
    slow: load or large data tests
