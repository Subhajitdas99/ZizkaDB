# Managed cloud (db.zizka.ai → Settings → API key)
ZIZKADB_API_KEY=zizkadb_live_xxxx
ZIZKADB_AGENT=my-agent

# Self-host (omit API key; dev key auto-injected on localhost)
# ZIZKADB_HOST=http://localhost:8000
